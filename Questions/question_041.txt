A data center has two processors handling incoming conversations. The first processor can filter 1200 messages per minute but only processes 75% of incoming data. The second processor handles the remaining messages at 800 messages per minute but extends processing time by 25% for each message. If 2000 conversations arrive per minute and the system runs for 4 hours, calculate the total number of unprocessed messages in the queue at the end of the operation.